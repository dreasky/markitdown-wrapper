# markitdown

## 依赖

- **markitdown**: 用于 Excel 转 MD
  - 自动通过 `run.py` 安装依赖

## 使用示例

Excel 文件不需要转 PDF，直接用 markitdown 转换：

```bash
python -m markitdown <绝对路径/file.xls> -o <绝对路径/file.md>
python -m markitdown <绝对路径/file.xlsx> -o <绝对路径/file.md>
```
